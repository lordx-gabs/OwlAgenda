<resources>
    <!-- Tema escuro do app -->
    <style name="Base.Theme.OwlAgenda" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/graybrown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">?android:attr/colorBackground</item>
        <item name="colorButtonNormal">@color/graybrown</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>